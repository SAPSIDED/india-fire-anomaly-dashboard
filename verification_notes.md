# Geospatial Interaction Verification Notes

The live workbench rendered three Google Maps-native marker elements with accessible labels for the Western India corridor, Gujarat process zone, and Northern India fringe. These markers are now map-attached objects rather than CSS percentage overlays, so their position follows latitude/longitude while the user pans or zooms. A direct browser click snapshot was interrupted by a transient browser reset; automated unit, type, and production-build tests are passing.

The subsequent live interaction test successfully selected the Western India map marker and opened the **Concurrent Conditional Verification** modal. The modal displayed the selected zone coordinates and a loading state that explicitly states that FIRMS, OSM industrial context, seven-day persistence, and weather are being queried concurrently.

The completed modal reported NASA FIRMS, OSM industrial context, and seven-day persistence as **UNAVAILABLE**, while weather context was **AVAILABLE**. Its final state was **“Live conclusion withheld”**, demonstrating that unavailable sources block an industrial-fire conclusion. The three geographic marker controls remained in the live DOM after using the map zoom control.

After closing the verifier and activating the Google Maps zoom-in control, the map moved to a tighter India view while the three risk-zone markers remained attached to their geographic locations. This directly verifies that the markers no longer use screen-relative percentages and remain stable during zooming.

With the map focused, a live keyboard ArrowRight pan was issued. The live map continued to expose all three labelled geographic marker controls after that pan, confirming that their locations are managed by Google Maps rather than page-relative overlay coordinates.

An automated synthetic pointer-drag attempt did not change the measured marker screen position, so it is not sufficient evidence of a completed physical pan. The geographic-marker implementation is in place, but the final pan task remains open for a direct manual pan check in the preview.

The final controlled Google Maps pan check used the live map instance itself. The map centre moved from **22.4°N, 78.2°E** to **22.4°N, 84.7918°E**, while the Western India marker moved on screen from **x=344** to **x=194** at the same vertical position. This confirms that the marker follows the map’s geographic transform during a genuine pan rather than remaining at a fixed page coordinate.

After the extraction hardening, a live map-zone click opened the verifier and displayed its bounded concurrent-loading state while FIRMS, OSM context, persistence, and weather checks executed. The selection remained open rather than crashing during delayed upstream calls.

The verifier was reloaded after the parallel Overpass-fallback change and a fresh zone check was started. The same map selection remained stable while the concurrent source requests were in progress, ready for the final bounded-result inspection.

The fresh verification run completed after the eight-second live-evidence window. The visible modal labelled each late source as pending/unavailable and displayed **“Evidence pending — sources still delayed”** with a clear statement that no industrial-fire conclusion had been issued. The map selection and Return to map control remained usable; no crash or indefinite loading occurred.

After the permanent relay configuration and FireGuard server restart, the browser frontend was opened against the workbench and the Western India candidate’s concurrent verification was started. The modal entered its expected loading state, initiating the frontend-to-backend leg of the permanent relay verification.

The completed browser verification returned **AVAILABLE** live evidence for NOAA-20, independent NOAA-21, Google Places industrial context, NOAA-20 seven-day persistence, and weather. The final live result was **“No current FIRMS thermal detection”** for the selected location—not an unavailable or pending state—so the application correctly declined an industrial-fire conclusion on the real current data. This confirms the full frontend → FireGuard backend → permanent Cloudflare relay → official NASA FIRMS → backend → frontend path.

The active browser modal was explicitly searched for the completed verdict text and returned one match: **“LIVE SCREENING RESULT No current FIRMS thermal detection — The live local one-day FIRMS search found no thermal detection; an industrial-fire conclusion is not supported.”** This is a direct browser-visible confirmation of the relay-backed response.

The remaining external requirement for a **confirmed industrial-fire incident** is an authority, emergency-service, facility, or on-site incident feed. The live relay now supplies authentic satellite, industrial-context, persistence, and weather evidence; it intentionally classifies only a screened thermal candidate when those sources agree. It does not invent on-site confirmation where no such authoritative input has been integrated.

After the light thermal-canvas presentation update, the live browser map remained interactive with its three map-native zone controls, and the **Run source verification** action opened the modal successfully. The same backend call completed within the bounded evidence window with the conservative result **“Evidence pending — live source delayed.”** NOAA-20 and NOAA-21 current records were marked **UNAVAILABLE** for this attempt; industrial context, weather, and the authority-evidence record were available, while persistence/cross-platform checks used cached evidence. The modal stated that no industrial fire had been inferred from missing live data. This records a completed post-redesign conditional-check outcome, not a UI-only modal-open check.

After restarting the development service to remove the stale module cache, a fresh browser run loaded all three map-native candidate controls and completed the conditional-verification modal without console errors. The returned live verdict was **“No current FIRMS thermal detection”** with the detail: **“The live local one-day FIRMS search found no thermal detection; an industrial-fire conclusion is not supported.”** NOAA-20, NOAA-21, industrial context, persistence, weather, and authority-ledger availability were recorded as **AVAILABLE**; the cross-platform comparison was **NO_CROSS_PLATFORM_MATCH**. The prior `incidentEvidence` module-export error did not recur during this completed run.

The upgraded historical panel was tested in both evidence-safe modes. In the fresh **NOT QUERIED** preview, a direct browser selection changed the visible callout from **25 Aug** to **21 Aug** without making a data request. A subsequent live verification returned seven official day buckets with **0 FIRMS detections** each; the history panel showed the **AVAILABLE** source state, seven zero-observation markers, zero total detections, zero active days, and zero peak-day count. Selecting the rendered **24 Aug** live button updated the active marker and callout to **“24 Aug — 0 detections in the local 8 km screening radius”** after React settled. This confirms the graph remains visibly interactive without inventing historical observations.

The direct preview interaction was hardened by responding on pointer press as well as click. In a fresh browser session, the default **25 Aug** NOT QUERIED callout changed to **21 Aug** after selecting the visible 21 Aug marker through the normal browser control, confirming the marker no longer depends on console-dispatched events. The production build and test suite remained clean after this interaction refinement.

The browser automation driver did not assign focus when using its direct element-click path (its active element remained `BODY`), so the intrinsic button handler was independently exercised through the rendered browser DOM. The recorded selection transitioned from **“QUERY-WINDOW PREVIEW 25 Aug”** to **“QUERY-WINDOW PREVIEW 21 Aug”** with the active marker changing from `2026-08-25: not queried` to `2026-08-21: not queried`. This confirms the actual React selection handler and visible callout state are correct; the remaining limitation was the automation driver’s focus behavior rather than the graph implementation.

For Stage 2, a non-test request to the existing `corroboration.run` procedure at **27.13°N, 73.33°E** returned HTTP 200 with the additive `longTermHistory` field present: `state: available`, `totalDetectionCount: 0`, `firstSeen: null`, `lastSeen: null`, and `activeMonths: 0`. A read-only query of the matching 8 km `detectionHistory` window returned the same empty result. This is an authentic empty-history readback: the current live request found no local FIRMS detection, so no synthetic row was inserted merely to demonstrate persistence.

For the required non-synthetic Stage 2 proof, the configured authenticated official relay’s NOAA-20 seven-day WFS response supplied the first current Indian coordinate at **20.79179°N, 85.25556°E**. Two non-test calls to the unchanged `corroboration.run` procedure returned HTTP 200 and live `firmsHistory` evidence of **8** local detections. The first response read back `longTermHistory` as `state: available`, `totalDetectionCount: 8`, `firstSeen: 2026-08-19`, `lastSeen: 2026-08-19`, and `activeMonths: 1`; the repeated request returned the same totals. A read-only database query over the matching 8 km window found **8 stored rows** and **8 distinct latitude/longitude/date keys**, confirming that the repeated live capture did not create duplicates. No credentials or synthetic records were used, and the Stage 1 classification module was not changed.

An additional official **24-hour** NOAA-20 WFS query subsequently returned a real Indian detection at **29.45627°N, 76.89240°E**, acquired on **2026-08-25**. The unchanged `corroboration.run` request did not expose it through `firmsCurrent`: its Area endpoint completed first with an available zero-row response, so the existing `Promise.any` path did not wait for the parallel WFS fallback. The procedure therefore returned `firmsCurrent: available, detections: 0`, despite the official 24-hour WFS row; it did write and read back 13 real seven-day rows for the coordinate. This does not satisfy the required current-window in-flow proof, so the two corresponding Stage 2 checklist items remain open. The test suite (23 tests), typecheck, and production build passed without modifying the fetch or Stage 1 classification logic.

With the subsequently approved non-blocking response-selection refinement, a valid local detection now wins as soon as it arrives, rather than allowing an earlier zero-row response to mask it or waiting for a hanging sibling route. A deterministic timing test covers the fast-zero, positive-WFS, hanging-sibling case. The existing Area, India-country, and WFS relay requests, retry policy, cache behavior, response shape, and Stage 1 classification remained in place.

For the final genuine current-window proof, the official NOAA-20 24-hour WFS feed supplied a previously unstored Indian coordinate at **15.38928°N, 75.22285°E** with acquisition date **2026-08-25**. A read-only preflight query found zero matching stored rows. Two non-test calls to the live `corroboration.run` procedure then each returned HTTP 200 with `firmsCurrent: { state: available, detections: 1 }` and `longTermHistory: { state: available, totalDetectionCount: 1, firstSeen: 2026-08-25, lastSeen: 2026-08-25, activeMonths: 1 }`. The post-run read-only database query found exactly **one** stored row and **one** distinct location/date key. This proves live 24-hour capture, database write/read-back, and duplicate safety without mock data or credentials in output.

For the additive land-cover source, a non-test live `corroboration.run` response at **15.38928°N, 75.22285°E** returned HTTP 200 and the independent field `landCover: { landCoverClass: cropland, source: Esri Sentinel-2 10m Land Use/Land Cover Time Series }`. The pre-existing Stage 1 classification output remained present and unchanged by the field; this run returned the existing rule-based `likely_wildfire_vegetation` classification without using land-cover evidence as an input.

For the India-wide stored hotspot map, the preferred official FIRMS country request was attempted first but currently returned `Invalid API call`; the approved official NASA FIRMS NOAA-20 WFS India-bounds fallback then returned authentic rows. The immediate startup refresh stored **85** rows with source `firms-wfs-india-fallback` at **2026-08-26T04:02:27Z**. A second startup refresh stored **85** rows at **2026-08-26T04:04:24Z** with new row IDs **30001–30085**, confirming atomic replacement rather than accumulation. The count did not change because the official 24-hour response was unchanged between the two checks; no rows were fabricated to force a difference. The live map rendered stored current observations and labelled the active source as **NASA FIRMS WFS fallback · India bounds** with its refresh timestamp. Map visits read the database-backed tRPC snapshot and do not trigger a live FIRMS request.

The physical table was then safely renamed to the exact required `india_hotspot_snapshot` name without dropping its data. After restart, the immediate official fallback refresh stored **85** rows in that table at **2026-08-26T04:08:06Z** with source `firms-wfs-india-fallback` and row IDs **60001–60085**. The full regression suite, typecheck, and production build passed afterward: **35 tests**, no TypeScript errors, and a successful build.

The published project-level Heartbeat `fireguard-india-hotspot-refresh` then completed its first managed callback successfully at **2026-08-26T04:48:54Z**. The authenticated POST to `/api/scheduled/refreshIndiaHotspots` returned HTTP **200** in **1,118 ms** with `{ ok: true, rowCount: 85, source: firms-wfs-india-fallback }`. The post-run `india_hotspot_snapshot` read-back showed 85 rows, the disclosed fallback source, the same 04:48:54 refresh timestamp, and newly replaced row IDs **120001–120085**. This confirms the durable platform schedule, the failure-safe handler, and actual atomic snapshot replacement after publication.

On 2026-08-26, the original managed task later accumulated 34 platform callback timeouts, each recorded as HTTP 0 with a 30-second `StartToClose timeout`. It was replaced with a clean project-level task using the same callback and `0 */20 * * * *` schedule. The replacement task `KpqbheuCc5ER2F2fc75gqP` completed its first production execution successfully at **2026-08-26T20:31:24Z**, returning HTTP **200** in **2,937 ms** with `{ ok: true, rowCount: 164, source: firms-wfs-india-fallback }`. Production request diagnostics confirmed the callback carried the expected `app_session_id` cookie; temporary diagnostics were removed after the confirmation.

The replacement job completed a second successful callback at **2026-08-26T20:49:34Z**, returning HTTP **200** in **4,501 ms** with the same 164-row official fallback snapshot and no error. The original task was deleted, so no further callbacks can originate from it. Because the two initial replacement executions were only about 18 minutes apart during platform registration, the active job was normalized to the scheduler service’s accepted 30-minute expression `*/30 * * * *`; its next execution was recorded as **2026-08-26T21:30:00Z**. This keeps the requested 20–30 minute rate bound explicit despite registration-time dispatch jitter.
